## 问题描述：

在 o.sg 环境使用标准运维 MCP（任务执行 MCP）时，AI 调用后返回数据明显不符合实际，疑似模型幻觉（乱编数据）。

## 解决方法：

切换使用**标准运维通用 MCP Server**（而非任务执行 MCP），并注意模型选择，避免使用效果较差的 qwen 模型。

**操作步骤：**

1. 在 BKAIDEV 中申请接入**标准运维通用 MCP Server**
2. 联系管理员审批后即可使用
3. 使用时避免选择 qwen 模型，推荐切换为其他效果更好的模型