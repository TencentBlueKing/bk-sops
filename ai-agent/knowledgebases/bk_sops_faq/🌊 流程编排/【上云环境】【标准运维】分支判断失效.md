### 问题：
用户的分支判断是：
```
("${Region}" != "Test" or "${Region}" != "Test env2")
```
### 原因：
这个逻辑有问题，不管Region是否不等于Test或者Test env2，返回的结果都是true
### 解决：
or 改成and
```
"${deploy_main_gass}" == "enable" and ("${Region}" != "Test" and "${Region}" != "Test_env2")
```
