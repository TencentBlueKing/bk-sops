# 全局变量的基础引用

> 公开基础说明。本文已重新编写，不沿用原始案例或截图转写。示例不代表任何实际部署配置。

变量以 KEY 区分，可在参数中通过 `${KEY}` 引用。同一变量可以用于多个参数位置；变量之间的引用不应形成循环。

例如定义普通文本变量 `sample_text`，基础引用写法为：

```text
${sample_text}
```

公开变量引擎还支持部分表达式；实际可用语法应以所用版本为准。这里不包含真实任务数据或跨系统操作流程。

---

公开来源编号：PUBLIC-02。来源链接和核验记录见包外复核报告。
